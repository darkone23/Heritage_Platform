#!/usr/bin/env bash

# TODO: can test if: "can sudo" instead
# skipping for now since we prefer non root users anyway
exit 0

# if [ `whoami` == "root" ]
# then
#     exit 0
# else
#     echo "You must be root to execute this command."
#     exit 1
# fi
